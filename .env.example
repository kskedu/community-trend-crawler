# community-trend-crawler 환경변수 예시 (값 없이 변수명만 — 실제 값은 GitHub Secrets/실행환경에만 주입)

# Supabase (기존 크롤러 공통)
SUPABASE_URL=
SUPABASE_KEY=

# 네이버 검색 오픈 API (실시간 이슈 브리핑 — P0-2에서 사용)
# crawler 실행 환경에만 주입. 프론트에는 절대 넣지 않는다.
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
